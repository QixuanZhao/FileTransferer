# File Transferer

Client Usage:

java FileTransferer filename destination-host[:port]

Server Usage:

java FileServerStart [port]

Files uploaded to server will be stored in server's working directory where the above command was launched.