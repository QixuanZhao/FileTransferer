# File Transferer

Client Usage:

java FileTransferer filename destination-host[:port]

Server Usage:

java FileServerStart [port]

Files uploaded to the server will be stored at the path where the above command was launched.